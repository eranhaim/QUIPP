# Leave empty in production/containerized deploys (nginx proxies /api/* to backend on same origin).
# Set to http://localhost:4000 for local Vite dev against a locally-running backend.
VITE_API_URL=http://localhost:4000
